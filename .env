# BatchData API Configuration
# API tokens are now loaded from batchapi.csv file

# Use sandbox environment (true) or production (false)
# Sandbox uses mock data for testing
# Set to false to use real BatchData API with production token
USE_SANDBOX=false

# Optional: Override API key from CSV (if not using batchapi.csv)
# BATCHDATA_API_KEY=your_api_key_here
